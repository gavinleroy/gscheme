;;-------------------------------------------
;;
;; Gavin Gray 01.2022
;;
;; GScheme
;;
;;-------------------------------------------

(env
  (dev
    (flags (:standard -warn-error -A -w -27))))

(library
 (name gscm)
 (libraries angstrom)
 (modules eval parser types util env string box) ;; exluding expander for now
 (inline_tests)
 (preprocess (pps ppx_inline_test ppx_let)))
